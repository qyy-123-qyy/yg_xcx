.tabs .tabs_title{
    display: flex;
    padding: 15rpx 0;
}
.tabs .tabs_title .title_item{
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 1;
}
.tabs .tabs_title .title_item.active{
    color: var(--themeColor);
    border-bottom: 5rpx solid currentColor;
}